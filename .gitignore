# Env / secrets
.env
.env.*
!.env.example
!.env.prod.example

# .NET
backend/**/bin/
backend/**/obj/
*.user

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Docker volumes (bind mounts, if any)
pgdata/

# Editor / OS
.vs/
.vscode/*
!.vscode/extensions.json
*.suo
Thumbs.db

# Documentación/contexto interno del proyecto (CLAUDE.md, notas de
# arquitectura, flujos de negocio, mapeos de Softbank) — no debe subirse
# al repo remoto, se queda solo local.
*.md

# Secretos reales de producción -- nunca commitear
.env.prod
